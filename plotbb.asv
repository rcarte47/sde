dt=.01;
x0=0;
tf=1;
t=0:dt:tf;
iter=100;
rng('shuffle');
bb=zeros(iter, length(t));

for i=1:iter
 bb(i,:)=bbridge(x0,tf,dt,t);
end

% plot(t,bb)
% y=.8399*sqrt(1-t);
% hold on
% plot(t,y)
% hold off